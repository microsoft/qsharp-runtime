[package]
name = "opensim_static"
version = "0.1.0"

[lib]
name = "opensim_static"
crate-type = ["staticlib"]

[dependencies]
opensim_runtime = { path = "../../../../Simulation/OpenSystems/runtime" }
