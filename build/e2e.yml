name: $(Build.Major).$(Build.Minor).$(date:yyMM).$(BuildId)

parameters:
- name: validation_level
  displayName: Validation Level
  type: string
  default: normal
  values:
  - minimal
  - normal
  - full

variables:
  E2E: "true"
  ENABLE_QIRRUNTIME: "false"
  NATIVE_SIMULATOR_BUILD_CONFIGURATION: "Release"

trigger: none

pr:
- main
- feature/*
- features/*
- release/*

resources:
  repositories:
  - repository: qdk
    type: github
    endpoint: github
    name: microsoft/qdk
    ref: refs/heads/main

extends:
  template: build/qdk-module-e2e.yml@qdk
  parameters:
    module: qsharp-runtime
    validation_level: ${{ parameters.validation_level }}
